import{S as o,o as i,c as a,a9 as n,B as l}from"./chunks/framework.DxG24V0C.js";const h=JSON.parse('{"title":"工具系统与执行管线：模型怎么把一个工具请求变成真实行动","description":"","frontmatter":{"title":"工具系统与执行管线：模型怎么把一个工具请求变成真实行动"},"headers":[],"relativePath":"deepseek/05-tools-pipeline.md","filePath":"deepseek/05-tools-pipeline.md","lastUpdated":1788319991000}'),d={name:"deepseek/05-tools-pipeline.md"},u=Object.assign(d,{setup(r){const t=[{question:"工具定义必须声明且单独一个 `output`（canonical 输出），最重要的作用是？",options:['约定"返回什么 JSON"，并独立 render 去呈现，让 JSON 与展示分离',"让 TypeScript 类型推断更精确","使工具储存空间更小","为了在模型面前隐藏参数"],correct:0,explanation:'output + render 让"规范值"与"展示"分离、可回放，也强化了 session "能记录但不可改"精神。输出规定属于职责分离，而非只在类型/空间向。'},{question:"为什么 `tools/schemas(scope)` 只把 name/description/parameters 投影给模型（而非整个定义）？",options:["避免把 execute/回调/元数据泄漏到 wire，也让模型只见最小签名","为了减少一次文件读取","因为模型只接受某种固定数据类型","为了不让工具注册到任何 scope"],correct:0,explanation:'白名单只发 name/description/parameters；execute 与回调属于执行层，不能入 wire。这是"不给模型看实现"的决定。'},{question:"哪个描述 100% 正确地说明单调守卫（guard）？",options:["只能返回拒绝理由，不能给出允许，防止被后续放行","能被之后的绕过","允许无论 guard 顺序都反转","可以随意替换参数"],correct:0,explanation:'单调 guard 返回值是 string|undefined 且无 allow 分支，即"只拒绝、不授权"，因此后来 listener 不能把已拒绝的放回。'},{question:"`tools/pre-execute` 的 `ask` 决策代表什么？",options:["调用需审批，只有被返回 allowed-once 才执行，否则 deny","无需任何许可直接允许","强制异步重试一次","把调用静默丢弃"],correct:0,explanation:'ask 只有在宿主审批（ctx.approval）给了 allowed-once 才跑；没有审批则降级为 deny。体现"宁可少放行不放错"。'},{question:"为什么 `dsh-tool-call-timeout-policy` 是一个插件而不是写死在工具注册表？",options:["超时是部署侧策略，可由插件按需装卸，不动核心目录","因为 timeout 不好实现","因为工具注册表不能写代码","因为模型不能理解 timeout"],correct:0,explanation:'`timeoutMs` 是声明式，真正的 deadline 由 `tools/execute` wrapper 插件强制；把行为性策略插件化正是"可单卸单装"的哲学，核心目录只保留机械骨架。'}];return(p,e)=>{const s=o("Quiz");return i(),a("div",null,[e[0]||(e[0]=n(`<h1 id="工具系统与执行管线-模型怎么把一个工具请求变成真实行动" tabindex="-1">工具系统与执行管线：模型怎么把一个工具请求变成真实行动 <a class="header-anchor" href="#工具系统与执行管线-模型怎么把一个工具请求变成真实行动" aria-label="Permalink to &quot;工具系统与执行管线：模型怎么把一个工具请求变成真实行动&quot;">​</a></h1><blockquote><p>本文基于 <code>dsh-v0.1.0-rc.7</code>。项目处于 developer preview，迭代很快，文中机制以该基线为准。</p></blockquote><p>前三篇里，agent 学会了&quot;跑一回合&quot;（agent-loop）、&quot;看上下文&quot;（session log）。但一个 agent 之所以能&quot;干活&quot;，关键在它能把一个模型请求变成<strong>真实动作</strong>：写文件、跑命令、查网页。这一层就是<strong>工具系统</strong>。</p><p><code>dsh</code> 的工具系统不只是一张&quot;名字 → 函数&quot;的注册表。它完整地做成了四件事，对应这四节的抽象：</p><ol><li><strong>注册</strong>：工具怎么挂进注册表、在哪个作用域可见；</li><li><strong>进 prompt</strong>：工具的 schema 怎么组合进系统提示，让模型&quot;知道有这个工具&quot;；</li><li><strong>被守卫执行</strong>：一次工具调用怎么走完&quot;允许/拒绝 → 超时/解耦 → 结果修正 → 落盘&quot;的管线；</li><li><strong>作为能力缝的消费方</strong>：工具是&quot;能力缝三元组&quot;里的 Consumer（下篇专讲）。</li></ol><p>先把这一篇的核心判断放在最前：</p><blockquote><p><strong><code>tools/*</code> 的生命周期事件（3 个可拦截 waterfall + 1 个只读结果通知），是 <code>dsh</code> 把&quot;模型要动真格&quot;这件事做成&quot;可插值、可拦截、可观测&quot;的关键。</strong> 工具的注册是注册，能不能真的执行要交给一整条<strong>分层管线</strong>去裁决，而不是一个 if/else。</p></blockquote><h2 id="一、工具注册表-ctx-tools-与作用域" tabindex="-1">一、工具注册表：<code>ctx.tools</code> 与作用域 <a class="header-anchor" href="#一、工具注册表-ctx-tools-与作用域" aria-label="Permalink to &quot;一、工具注册表：\`ctx.tools\` 与作用域&quot;">​</a></h2><p>工具都注册到 <code>ctx.tools</code>（一个 <code>ToolRuntime</code> 服务）。<code>packages/core/tools/README.md</code> 说得很清楚：</p><blockquote><p>Tool plugins register their schemas and executors; the agent loop executes each call through … the pipeline.</p></blockquote><p>注册一个工具就是调用 <code>ctx.tools.register(definition)</code>。一个 <code>ToolDefinition</code> 长什么样？它由&quot;模型可见的 schema&quot; + &quot;执行函数&quot; + &quot;输出声明&quot;组成。<code>defineTool()</code> 是给自家插件作者用的类型安全帮手，<code>docs/subsystems/tools.md</code> 里有一个最小例子：</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// packages/core/tools/README.md — defineTool 最小示例</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ctx.tools.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">register</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defineTool</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;read_file&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  description: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Read a file from disk.&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  parameters: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    path: { type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;string&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, required: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, description: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Absolute file path&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    offset: { type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;number&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    limit: { type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;number&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  output: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    schema: { type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;string&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    render</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">_args</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">value</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [{ type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;text&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, text: value }],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  async</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">args</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">exec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> readFile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(args.path, { encoding: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;utf8&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, signal: exec.signal })</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}))</span></span></code></pre></div><p>拆开三个要点：</p><ol><li><strong><code>output</code> 是强制声明的</strong>（mandatory canonical output declaration）。工具不仅声明&quot;我会收什么参数&quot;，还必须声明&quot;我会返回什么 canonical JSON value&quot;，以及怎么把它 <code>render</code> 成给模型的 content。这是第 4 篇&quot;model-visible ⟺ logged&quot;的具体延伸之一——工具输出也有一个可校验、可渲染的声明。</li><li><strong><code>execute</code> 只返回&quot;canonical JSON value&quot;</strong>，不含 UI。<code>output.render</code> 才是&quot;把 value 变成模型/UI 能看的 content&quot;的那个纯函数。执行与展示彻底分离。</li><li><strong>作用域</strong>。<code>register</code> 在谁上调用很重要：一个普通 plugin 的 context 注册的是<strong>全局</strong>工具；在一个 agent 的 <code>agent.ctx</code> 上注册只对<strong>那个 agent</strong> 可见、且能遮蔽同名全局工具。</li></ol><p><code>ctx.tools</code> 还提供 <code>restrict(filter)</code> 做 per-agent 的 allow/deny 遮蔽。注意 README 反复强调：&quot;This is live visibility composition, not an authority boundary&quot;——<strong>作用域上的工具</strong>可见性限制是&quot;路由/组成&quot;，不是安全边界（安全边界在 capability 缝 + 权限 policy，见第 6 篇）。</p><p>一句话记住注册层：</p><blockquote><p><strong>工具注册 = 声明&quot;我能干什么、我要什么参数、我能返回什么 canonical 值&quot;，并挂到全局或某个 agent 的作用域上；展示函数与执行函数彻底分离。</strong></p></blockquote><h2 id="二、schema-进-prompt-模型怎么-知道-有这些工具" tabindex="-1">二、schema 进 prompt：模型怎么&quot;知道&quot;有这些工具 <a class="header-anchor" href="#二、schema-进-prompt-模型怎么-知道-有这些工具" aria-label="Permalink to &quot;二、schema 进 prompt：模型怎么&quot;知道&quot;有这些工具&quot;">​</a></h2><p>工具注册了，下一步是让模型看到它们。<code>ctx.tools</code> 与 <code>system-prompt</code> 之间有一条自动约定：</p><blockquote><p>The registry automatically feeds its tool schemas into the system-prompt assembly via <code>ctx.systemPrompt.tools()</code>.</p></blockquote><p>具体地，<code>ctx.tools.schemas(scope)</code> 投影出&quot;该 scope 可见的所有工具&quot;的<strong>模型可见字段子集</strong>——只含 <code>name/description/parameters</code>，<strong>绝不含 <code>execute/finalizeContent/render</code> 等回调</strong>。这些 schema 汇入 system prompt 组装，最终随 <code>agent/request</code> 的 waterfall 进入每个 step 的请求。</p><p>这一步回答的是一个微妙问题：<strong>工具定义里既要有&quot;给机器执行的代码&quot;，又要有&quot;给模型看的 schema&quot;，二者绝不能混</strong>。<code>dsh</code> 的处理是&quot;白名单投影&quot;：<code>schemas()</code> 只把 <code>name/description/parameters</code> 投影到请求线，其余字段（执行函数、超时、展示回调、并发安全标记）<strong>一律不让进 wire</strong>。这样：</p><ul><li>模型拿到的永远是干净的、最小化的函数签名；</li><li>实现细节、回调、执行代码绝不泄漏到模型请求里。</li></ul><p>这里还能体会到 <code>isConcurrencySafe</code>、<code>timeoutMs</code> 这类<strong>元数据</strong>为什么必须&quot;永不 model-visible&quot;：它们属于执行/调度策略，不是模型调用工具所需的&quot;了解接口&quot;信息。</p><h2 id="三、执行管线-一条带守卫的水瀑" tabindex="-1">三、执行管线：一条带守卫的水瀑 <a class="header-anchor" href="#三、执行管线-一条带守卫的水瀑" aria-label="Permalink to &quot;三、执行管线：一条带守卫的水瀑&quot;">​</a></h2><p>这是工具系统的心脏。<code>ctx.tools.execute()</code> 把一次调用<strong>走完整条管线</strong>，<code>packages/core/tools/README.md</code> 给出了权威顺序：</p><blockquote><p>…run each call through <code>tools/pre-execute</code> (the extensible allow/deny gate) → monotonic registered guards → <code>tools/execute</code> (an around-dispatch wrapper for timeout/retry/metrics plugins) → <code>tools/post-execute</code> (inspect/replace the result, attach context) → the definition-owned <code>finalizeContent</code> boundary → the observe-only <code>tools/result</code> notification.</p></blockquote><p>翻译成一段话：</p><p><code>tools/pre-execute</code>（<strong>允许/拒绝/询问</strong>的可扩展闸门）→ <strong>单调守卫</strong>（谁也不能再放行）→ <code>tools/execute</code>（around-dispatch 包装：超时/重试/指标）→ <code>tools/post-execute</code>（检查/替换结果、附上下文）→ 工具自带 <code>finalizeContent</code> → 只读的 <code>tools/result</code>（落盘前最后通知）。</p><h3 id="三种决策-allow-deny-ask" tabindex="-1">三种决策：allow / deny / ask <a class="header-anchor" href="#三种决策-allow-deny-ask" aria-label="Permalink to &quot;三种决策：allow / deny / ask&quot;">​</a></h3><p><code>tools/pre-execute</code> 是一个 waterfall，每个 listener 返回一个 <code>PreToolDecision</code>：</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PreToolDecision</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;allow&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;deny&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">reason</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  |</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;ask&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">reason</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><ul><li><code>allow</code>：直接跑；</li><li><code>deny</code>：把这次调用变成一个错误结果；</li><li><code>ask</code>：<strong>只有在某个审批服务返回 <code>allowed-once</code> 之后才跑</strong>，否则也是 deny。没有审批服务时 ask 降级为 deny（<code>不允许默默放行</code>）。</li></ul><p>注意 <code>ask</code> 的设计——<code>dsh</code> 把**审批（approval）**做成工具管线的一个环节：前端（交互层）可以挂 <code>ctx.approval</code>，<code>tools/pre-execute</code> 的 ask 就调用它；没挂就降级为拒绝，宁可保守不漏，也不静默放行。</p><h3 id="单调守卫-不让已拒绝的再被批准" tabindex="-1">单调守卫：不让已拒绝的再被批准 <a class="header-anchor" href="#单调守卫-不让已拒绝的再被批准" aria-label="Permalink to &quot;单调守卫：不让已拒绝的再被批准&quot;">​</a></h3><p><code>ctx.tools.guard(guard)</code> 提供的是<strong>单调</strong>的执行守卫——即&quot;只能更加严格，不能更松&quot;。它的返回类型刻意<strong>没有 allow 分支</strong>（<code>(execution) =&gt; string | undefined</code>，返回字符串即拒绝）：因为只要有 deny 结果，&quot;后续 listener 就无法把它改回 allow&quot;；<code>undefined</code> 只说明&quot;保持原样&quot;。这是防&quot;守卫之间互相推翻&quot;的关键——<code>docs/subsystems/tools.md</code> 原话：</p><blockquote><p>Because guards have no allow result, listener ordering cannot turn a denial back into permission.</p></blockquote><p>一个工具调用的权限裁决，是这层&quot;让-拒绝-问 + 单调守卫&quot;共同完成的：可扩展的 allow/deny/ask 提供<strong>灵活性</strong>，单调守卫保证&quot;一旦拒绝就收不回来&quot;。</p><h3 id="around-dispatch-超时-重试-指标挂在这里" tabindex="-1">around-dispatch：超时/重试/指标挂在这里 <a class="header-anchor" href="#around-dispatch-超时-重试-指标挂在这里" aria-label="Permalink to &quot;around-dispatch：超时/重试/指标挂在这里&quot;">​</a></h3><p><code>tools/execute</code> 是 around-dispatch 的 waterfall：wrapper 包住真正的 body，可做超时、重试、指标。它重要的约束是——wrapper 只允许替换 <code>exec.signal</code>，<strong>不能去掉调用者取消信号</strong>（registry 会在 body 前把原始的 caller signal 重新融合回去），这样 wrapper 的数据不会让调用者失去取消能力。</p><p>超时是怎么挂进去的？<code>docs/subsystems/tools.md</code> 里 <code>ToolDefinition.timeoutMs</code> 明确说是<strong>声明性</strong>的、<strong>不是</strong>执行期强制：</p><blockquote><p><code>timeoutMs</code> on a definition is declarative only — the registry never enforces deadlines; enforcement requires the <code>@deepseek-ai/dsh-tool-call-timeout-policy</code> wrapper.</p></blockquote><p>也就是说：工具可以被声明&quot;我有一个 30s 的超时预算&quot;，但真正执行超时的是<strong>一个独立插件</strong>（<code>dsh-tool-call-timeout-policy</code>），它作为一个 <code>tools/execute</code> 的 wrapper 注册。这是&quot;插件哲学&quot;在工具栈的又一次贯彻——超时不是写在核心注册表里，而是作为一个可独立挂载/卸载的 guard/wrapper 插上去。</p><h3 id="post-execute-result-结果修正与观察" tabindex="-1">post-execute + result：结果修正与观察 <a class="header-anchor" href="#post-execute-result-结果修正与观察" aria-label="Permalink to &quot;post-execute + result：结果修正与观察&quot;">​</a></h3><p>跑完之后，<code>tools/post-execute</code> listener 可以<strong>修正结果</strong>：接受、替换 content 或 value、或 block 成带反馈的错误；随后 <code>finalizeContent</code>（工具自带）做<strong>最后一公里的 content-only</strong> 修正；最后由 <code>tools/result</code> <strong>只读通知</strong>观测最终的冻结结果。</p><p>这里有一个必须在意的设计取舍——**&quot;参数不可改&quot;**与&quot;结果可改&quot;：</p><ul><li><code>tools/pre-execute</code> <strong>故意不允许改写 <code>exec.arguments</code></strong>，因为参数已经被 logged 和 presented，改写会造成历史/审计/UI/执行彼此脱钩；</li><li>但 <code>tools/post-execute</code> 允许改 content（展示）而不改 canonical value，或反过来改 value 重算 content——这是策略层面的&quot;结果修正&quot;，与&quot;进线前的约定&quot;不同。</li></ul><p><strong>结论</strong>：执行的&quot;进&quot;（参数）和&quot;出&quot;（结果）在抽象上有非常不对等的自由度——<strong>进闸不可改（保真），出闸可改（策略）</strong>。这确保了历史一致（参数与所执行一致）、策略灵活（结果可被包装/遮蔽）。</p><h2 id="四、守卫、超时与重复提醒-插件在守什么" tabindex="-1">四、守卫、超时与重复提醒：插件在守什么 <a class="header-anchor" href="#四、守卫、超时与重复提醒-插件在守什么" aria-label="Permalink to &quot;四、守卫、超时与重复提醒：插件在守什么&quot;">​</a></h2><p>即便有 allow/ask/post 三层水面，<code>dsh</code> 还留了两个&quot;卫生守卫&quot;——它们是独立插件，不是硬编码在工具系统里：</p><ul><li><strong>超时</strong>：<code>dsh-tool-call-timeout-policy</code>（tools/execute 的 wrapper），遵守通知 <code>timeoutMs</code> 给某个工具设的 deadline；超了就把结果变 <code>TOOL_TIMEOUT</code>，并把错误交给模型自纠。</li><li><strong>重复提醒</strong>：guard 家族里的 loop-hygiene 插件 <code>@deepseek-ai/dsh-repeat-tool-reminder</code>——在一个工具被反复以相同参数调用时，注入&quot;重复了吗/卡死了吗&quot;的提示。注意它<strong>不是</strong> <code>ctx.tools.guard()</code> 那种单调守卫，而是挂在 <code>tools/post-execute</code> + <code>agent/pre-step</code> 上做 <strong>advisory（建议式）提醒</strong>：它只提示、不 veto，因为&quot;重复调用&quot;既可能是合法轮询、也可能真死循环，这个判断留给模型/用户去定。</li></ul><p>它们为什么是<strong>插件</strong>而非硬编码？因为：</p><ol><li><code>timeoutMs</code> 是<strong>声明式</strong>的，真正的 deadline 由 <code>@deepseek-ai/dsh-tool-call-timeout-policy</code> 作为一个可独立安装/卸载的 <code>tools/execute</code> wrapper 去强制执行——它不在注册表内，而是插在管线上；</li><li>&quot;是否/多久算超时&quot;&quot;重复多少次该提醒&quot;是<strong>部署时可变</strong>的策略——项目能按产品/配置项，而不是改核心代码。</li></ol><p>这和第 2 篇的&quot;注册即副作用、可逆卸载&quot;一脉相承：<strong>凡是&quot;行为性&quot;的东西都尽量做成插件，核心目录只保留机械的执行管线骨架。</strong></p><h2 id="五、工具-capability-缝的-consumer" tabindex="-1">五、工具 = capability 缝的 Consumer <a class="header-anchor" href="#五、工具-capability-缝的-consumer" aria-label="Permalink to &quot;五、工具 = capability 缝的 Consumer&quot;">​</a></h2><p>这是把工具接到第 6 篇的桥。前几节一直在讲&quot;工具怎么注册、怎么执行&quot;，但必须补一句定位：<strong>在 <code>dsh</code> 里，一个工具通常是某个 capability 缝的「Consumer（消费方）」</strong>，而不是&quot;夹在代码里的一个东西&quot;。</p><p>看第 2 篇就埋过伏笔的 <code>docs/capability-seams.md</code>：一个 seam 是&quot;Service Definition（接口）+ Provider（实现）+ Consumer（消费方）&quot;的三元组，而 Consumer <strong>通常是一个 model-facing 工具</strong>——它一端接进&quot;agent / 模型怎么看到这个能力&quot;，另一端接进&quot;实际执行这个能力的 provider&quot;。</p><p>举个例子，<code>dsh-tool-fs</code>（见包 README）就是 fs 这个 capability 的 Consumer：<strong>它向模型暴露&quot;读写文件&quot;的工具 schema，实际干活时通过 <code>ctx.fs</code> 服务（缝）去调用本地 provider。</strong> 所以：</p><blockquote><p>工具&quot;层&quot;与&quot;能力缝&quot;层不是一个概念。<strong>工具是能力的 Consumer，能力缝是&quot;接口 + 实现 + 消费&quot;三件套。</strong> 理解工具，最终要理解它吃哪一种缝、替换哪种 provider 时它跟着一起走。</p></blockquote><p>这句话把 5、6 两篇锁在一起：工具（第 5 篇）是消费方，Capability 缝（资源第 6 篇）是&quot;三件套&quot;。</p><p>当然，也有<strong>纯工具</strong>不属于某个 seam（比如 <code>todo_write</code> 这种纯粹的工具，它不需要一个可替换的 provider）——<code>todo</code> 在 <code>packages/todo/</code> 单独成 package 而没有缝。所以&quot;工具不一定都要吃 seam&quot;，但&quot;能力缝的 Consumer 通常是工具&quot;。这层区分在第 6 篇会讲得更细。</p><h2 id="六、与三栏对比-工具的-守卫-放哪" tabindex="-1">六、与三栏对比：工具的&quot;守卫&quot;放哪 <a class="header-anchor" href="#六、与三栏对比-工具的-守卫-放哪" aria-label="Permalink to &quot;六、与三栏对比：工具的&quot;守卫&quot;放哪&quot;">​</a></h2><table tabindex="0"><thead><tr><th>维度</th><th>OpenCode</th><th>Codex</th><th>DeepSeek Harness（<code>dsh</code>）</th></tr></thead><tbody><tr><td>工具注册</td><td>Tool.Def 接口</td><td>tool executor</td><td><strong>ctx.tools.register（typed schema DSL）</strong></td></tr><tr><td>工具进 prompt</td><td>手动拼入 context</td><td>executor 反射</td><td><strong>自动经 <code>ctx.systemPrompt.tools()</code></strong></td></tr><tr><td>执行管线</td><td>一层 executor</td><td>一层 event</td><td><strong>pre-execute → guard → execute → post-execute → result</strong></td></tr><tr><td>是否可碰</td><td>专业里改</td><td>改动大</td><td>单功能都可写插件挂</td></tr></tbody></table><p>参照点：dsh 最大的不同是把工具从&quot;一个函数&quot;抬成了&quot;一条<strong>可插值、可拦截、可观察</strong>的管线&quot;。工具并不&quot;天生安全&quot;，需要靠&quot;受调的授权语义 + 单调守卫 + 可观测事件&quot;去约束它既敢放它去执行，又能在它上面挂审批、超时、重复提醒等一堆策略插件。<strong>它把&quot;给模型一个工具&quot;从&quot;一次性决定&quot;变成了&quot;每一步都可以裁决&quot;。</strong></p><h2 id="七、总结-工具系统让你做-plugin-的三个平面" tabindex="-1">七、总结：工具系统让你做 plugin 的三个平面 <a class="header-anchor" href="#七、总结-工具系统让你做-plugin-的三个平面" aria-label="Permalink to &quot;七、总结：工具系统让你做 plugin 的三个平面&quot;">​</a></h2><p>工具系统把一件事展开成三个平面，你不必混：</p><ol><li><strong>声明平面</strong>：<code>defineTool</code> 注册 schema + 强制 <code>output</code>；模型只看到 <code>name/description/parameters</code>。</li><li><strong>执行平面</strong>：<code>pre-execute</code>（allow/deny/ask）+ guard（单调）→ <code>execute</code>（可改 signal）→ <code>post-execute</code>（可改结果）→ <code>finalizeContent</code> → 只读 <code>tools/result</code>。<strong>成败靠这条管线裁决：进闸的参数不可改（保真），出闸的结果可被策略修正（灵活）。</strong></li><li><strong>projector/观察平面</strong>：<code>schemas()</code> 按 scope 投影模型可见字段；<code>presentCall/presentResult</code> 让工具自己决定 UI <strong>怎么展示</strong>；一切结果都沿线沉淀为 <code>tool/call</code>/<code>tool/result</code> 会话事件（回到第 4 篇的&quot;什么都被记录&quot;）。</li></ol><p>这三个平面的分割，是 <code>dsh</code> 把&quot;模型在用工具&quot;从一个&quot;无从下手&quot;的黑盒，变成一个<strong>每一环可观测、可拦截、可审计的流程</strong>。它把第 2 篇&quot;一切皆插件&quot;从&quot;结构层&quot;落实到了&quot;单个动作层&quot;：连一次工具调用都能被你在它上面挂审批、挂超时、挂 ask。</p><h3 id="下一篇" tabindex="-1">下一篇 <a class="header-anchor" href="#下一篇" aria-label="Permalink to &quot;下一篇&quot;">​</a></h3><p>工具系统解释了&quot;怎么执行&quot;。那&quot;这些工具到底在调用谁&quot;&quot;为什么换 provider 天下动？&quot;——这才是 <code>dsh</code> 真正厉害的一层：**capability 缝（capability seam），三元组与 provider 互换。**走到第 6 篇。</p><h2 id="章节小测" tabindex="-1">章节小测 <a class="header-anchor" href="#章节小测" aria-label="Permalink to &quot;章节小测&quot;">​</a></h2>`,71)),l(s,{questions:t})])}}});export{h as __pageData,u as default};
