import{S as i,o,c as h,a9 as d,G as c,E as t,a as n,B as s,aa as r}from"./chunks/framework.DxG24V0C.js";const C=JSON.parse('{"title":"Claude Code 整体架构：51 万行源码全景","description":"","frontmatter":{"title":"Claude Code 整体架构：51 万行源码全景"},"headers":[],"relativePath":"claudecode/01-overview.md","filePath":"claudecode/01-overview.md","lastUpdated":1786085271000}'),k={name:"claudecode/01-overview.md"},m=Object.assign(k,{setup(u){const l=[{question:"Claude Code 选择 Bun 而非 Node.js 作为运行时，最关键的原因是什么？",options:["显著降低启动阶段的进程加载时间开销","原生执行 TypeScript 省去独立转译步骤","通过编译期死代码消除实现特性按需裁剪","在包管理性能方面优于 Node.js 体验"],correct:2,explanation:"三个原因都成立，但编译期死代码消除是最关键的——它让 Claude Code 能在同一个代码库维护尚未公开的特性（如 Coordinator、Kairos 等近二十个 feature flag），打包时按 flag 裁剪出不同产物。这是能支撑 51 万行代码规模且灰度策略灵活的工程基础。"},{question:"Claude Code 分层架构中，`hooks/` 目录的 104 个文件在分层中扮演什么角色？",options:["直接归入表现层，仅承担组件渲染职责","完全属于业务逻辑层，专注编排服务层 API 调用","物理归入基础设施层，运行期横跨表现层与业务逻辑层","独立于四层架构之外，自成第五层抽象层次"],correct:2,explanation:"hooks/ 物理上在基础设施层，但运行期绑定在组件树上，既调用业务逻辑层的 QueryEngine，又调用服务层的各种服务，还属于表现层的一部分。它是事实上的跨层胶水，CC 的「Hook 即业务」风格让逻辑天然随 UI 生命周期运行。"},{question:"Claude Code 启动流程中，`startKeychainPrefetch()` 和 `startMdmRawRead()` 被放在所有 import 之前执行的主要目的是什么？",options:["避免模块间循环依赖导致的运行时加载错误","在导入期并行预热 IO 以将等待藏于 CPU 时间","确保钥匙串与 MDM 数据在模块求值前全部就绪","优先验证身份后再加载模块以满足安全合规要求"],correct:1,explanation:"这三个副作用在所有 import 之前执行，把「IO 等待」与「CPU 导入」重叠起来。钥匙串读取和 MDM 查询是 IO 等待、天然可并行，而模块导入是 CPU 密集且不可压缩的。等到模块导入完成时，这些 IO 操作通常已就绪，只需轻量 await。"},{question:"对比 Claude Code、OpenCode 和 Codex，Claude Code 最独特的架构特征是？",options:["基于 React 与自研 Ink 实现终端 UI 渲染","以 continuation polling 驱动主循环","内建跨平台沙箱以实现默认的文件系统隔离","使用 Rust 编译原生二进制以追求极致性能"],correct:1,explanation:"Claude Code 的主循环是 continuation-driven polling——循环走向完全由 API 响应内容驱动，模型返回 tool_use 就继续，end_turn 就终止。这与 OpenCode 的显式 7 步循环和 Codex 的 Tokio event loop 形成根本差异。核心原因是 Anthropic 流式 API 天然适合「边收边处理」的模式。"}];return(g,e)=>{const a=i("Mermaid"),p=i("Quiz");return o(),h("div",null,[e[2]||(e[2]=d(`<h1 id="claude-code-整体架构-51-万行源码全景" tabindex="-1">Claude Code 整体架构：51 万行源码全景 <a class="header-anchor" href="#claude-code-整体架构-51-万行源码全景" aria-label="Permalink to &quot;Claude Code 整体架构：51 万行源码全景&quot;">​</a></h1><p>Claude Code 是 Anthropic 官方出品的终端 AI 编程助手，也是当前最成熟的终端编程 Agent 之一。本系列开篇从宏观视角切入：先看清它的技术栈、源码组织、启动流程与分层架构，再为后续逐章精读奠定骨架。本文所有源码引用均可在 <code>src/</code> 目录下 grep 验证。</p><h2 id="一、项目概览" tabindex="-1">一、项目概览 <a class="header-anchor" href="#一、项目概览" aria-label="Permalink to &quot;一、项目概览&quot;">​</a></h2><h3 id="_1-1-定位与规模" tabindex="-1">1.1 定位与规模 <a class="header-anchor" href="#_1-1-定位与规模" aria-label="Permalink to &quot;1.1 定位与规模&quot;">​</a></h3><p>Claude Code 是一个<strong>纯客户端应用</strong>：全部代码运行在用户本机，自身不包含任何模型推理逻辑，只负责构造请求、调用 Anthropic API、解析流式响应、调度工具并渲染终端 UI。模型能力完全来自远端，本地只做编排（orchestration）。</p><p>源码规模经实测统计（<code>src/</code> 下所有 <code>.ts</code>/<code>.tsx</code>）：</p><ul><li>代码总量约 <strong>512,664 行 TypeScript</strong></li><li>文件数量约 <strong>1,884 个</strong></li><li>顶层条目 <strong>53 个</strong>（35 个目录 + 18 个文件）</li><li>入口文件 <code>src/main.tsx</code> 单文件 <strong>4,683 行</strong></li></ul><p>这个体量在终端 Agent 中属于第一梯队。作为对照，下表横向比较 Claude Code、OpenCode 与 Codex 三个同类工具：</p><table tabindex="0"><thead><tr><th>维度</th><th>Claude Code</th><th>OpenCode</th><th>Codex</th></tr></thead><tbody><tr><td>代码规模</td><td>~512k 行 TS / 1,884 文件</td><td>~440k 行 TS / 2,100 文件</td><td>~950k 行 Rust（含 vendored）</td></tr><tr><td>运行时</td><td>Bun</td><td>Bun / Node.js</td><td>原生二进制（Rust 编译）</td></tr><tr><td>UI 框架</td><td>React + Ink（自研 <code>ink/</code>）</td><td>SolidJS + <code>@opentui/solid</code></td><td>ratatui（Rust TUI）</td></tr><tr><td>状态管理</td><td>自定义 store + React Context</td><td>SolidJS 信号（<code>solid-primitives</code>）</td><td>Tokio + 原生结构体</td></tr><tr><td>工具数量</td><td>~40</td><td>~25</td><td>~20</td></tr><tr><td>主循环风格</td><td>continuation-driven polling</td><td>事件驱动 + async 流式</td><td>Tokio event loop</td></tr><tr><td>沙箱</td><td>无</td><td>无</td><td>跨平台沙箱</td></tr><tr><td>开源状态</td><td>未公开</td><td>开源</td><td>开源</td></tr></tbody></table><p>需要特别说明的是：OpenCode 并未使用 React + Ink，而是采用 <strong>SolidJS + <code>@opentui/solid</code></strong> 构建终端 UI——这是三者中技术选型差异最大的一处。Codex 则是纯 Rust 实现，借助 <code>ratatui</code> + <code>tokio</code> 走编译型原生路线。</p><h3 id="_1-2-纯客户端的本质" tabindex="-1">1.2 纯客户端的本质 <a class="header-anchor" href="#_1-2-纯客户端的本质" aria-label="Permalink to &quot;1.2 纯客户端的本质&quot;">​</a></h3><p>理解 Claude Code 的第一原则是：<strong>它是一个编排器（orchestrator），不是推理引擎</strong>。本地代码承担四件事：</p><ol><li>组织上下文（系统提示、历史消息、工具定义、记忆文件）</li><li>向 Anthropic API 发起流式请求</li><li>解析返回的 <code>tool_use</code> 块，在本地执行工具（读写文件、运行命令、调用 MCP）</li><li>把工具结果回填为 <code>tool_result</code>，继续下一轮流式调用，直到模型返回 <code>message_stop</code></li></ol><p>这条「请求—工具—回填—再请求」的链路构成整个 Agent 的心跳。后文的主循环、工具系统、压缩机制都是围绕它展开的。</p><p>需要强调的一点是「纯客户端」并不等于「单进程」。Claude Code 内部仍会派生子进程执行 shell 命令、调用 MCP server、运行 LSP；但所有这些子进程都是<strong>工具执行的载体</strong>，而非模型推理的承载者。模型能力始终来自远端 API，本地只决定「什么时候调、调什么工具、怎么把结果拼回去」。这种「重编排、轻计算」的定位，直接决定了它为何能把 51 万行代码几乎全部花在 UI、工具、权限、上下文管理与流程编排上，而几乎没有自研模型相关代码。</p><h2 id="二、技术栈细节" tabindex="-1">二、技术栈细节 <a class="header-anchor" href="#二、技术栈细节" aria-label="Permalink to &quot;二、技术栈细节&quot;">​</a></h2><h3 id="_2-1-bun-运行时与-bun-bundle-死代码消除" tabindex="-1">2.1 Bun 运行时与 <code>bun:bundle</code> 死代码消除 <a class="header-anchor" href="#_2-1-bun-运行时与-bun-bundle-死代码消除" aria-label="Permalink to &quot;2.1 Bun 运行时与 \`bun:bundle\` 死代码消除&quot;">​</a></h3><p>Claude Code 选择 <strong>Bun</strong> 而非 Node.js 作为运行时，原因有三：</p><ul><li><strong>启动速度</strong>：Bun 启动开销显著低于 Node.js，配合下文将看到的并行预热策略，能把首屏时间压到数百毫秒级。</li><li><strong>原生 TypeScript</strong>：无需单独的编译/转译步骤，直接执行 <code>.ts</code>/<code>.tsx</code>。</li><li><strong>编译期死代码消除</strong>：这是最关键的一项。</li></ul><p>Bun 提供 <code>bun:bundle</code> 模块的 <code>feature()</code> 函数，用于在打包时按 feature flag 裁剪代码。<code>src/main.tsx</code> 顶部即导入它：</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { feature } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;bun:bundle&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// src/main.tsx:22</span></span></code></pre></div><p>随后大量模块以「条件 require」的形式引入，使未启用的特性在最终产物中完全消失：</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Dead code elimination: conditional import for COORDINATOR_MODE</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> coordinatorModeModule</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> feature</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;COORDINATOR_MODE&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  ?</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> require</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;./coordinator/coordinatorMode.js&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> typeof</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;./coordinator/coordinatorMode.js&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  :</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Dead code elimination: conditional import for KAIROS (assistant mode)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> assistantModule</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> feature</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;KAIROS&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  ?</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> require</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;./assistant/index.js&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> typeof</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;./assistant/index.js&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  :</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> kairosGate</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> feature</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;KAIROS&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  ?</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> require</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;./assistant/gate.js&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> typeof</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;./assistant/gate.js&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  :</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>上述片段位于 <code>src/main.tsx:74-81</code>。注意三个细节：</p><ol><li>用 <code>require()</code> 而非 <code>import</code>，是为了<strong>避免循环依赖</strong>——<code>main.tsx</code> 是顶层入口，很多模块会反向引用它，顶层 <code>import</code> 会立即执行副作用形成环；<code>require()</code> 推迟到运行时按需求值。</li><li><code>as typeof import(&#39;...&#39;)</code> 是一个「类型保留、运行时按需」的技巧：编译期保留完整类型信息供 TypeScript 检查，运行期只有 <code>feature()</code> 为真时才真正加载模块。</li><li>仓库内能 grep 到的 feature flag 至少有十余个：<code>COORDINATOR_MODE</code>、<code>KAIROS</code>、<code>KAIROS_BRIEF</code>、<code>KAIROS_CHANNELS</code>、<code>TRANSCRIPT_CLASSIFIER</code>、<code>DIRECT_CONNECT</code>、<code>SSH_REMOTE</code>、<code>LODESTONE</code>、<code>BG_SESSIONS</code>、<code>CHICAGO_MCP</code>、<code>WEB_BROWSER_TOOL</code>、<code>PROACTIVE</code>、<code>BRIDGE_MODE</code>、<code>AGENT_MEMORY_SNAPSHOT</code>、<code>CCR_MIRROR</code>、<code>UDS_INBOX</code>、<code>UPLOAD_USER_SETTINGS</code>。它们对应 Coordinator 协调模式、Kairos 助手模式、转录 AI 分类器、SSH 远程、内置浏览器工具等仍处于灰度阶段的特性。</li></ol><h3 id="_2-1-1-条件-require-为何规避循环依赖" tabindex="-1">2.1.1 条件 require 为何规避循环依赖 <a class="header-anchor" href="#_2-1-1-条件-require-为何规避循环依赖" aria-label="Permalink to &quot;2.1.1 条件 require 为何规避循环依赖&quot;">​</a></h3><p><code>main.tsx</code> 是整个进程的顶层入口，几乎所有业务模块（工具、命令、服务、UI 组件）都会反向引用它导出的全局状态或工具集合。如果用顶层 <code>import</code> 加载这些模块，ESM 的静态导入会在模块求值阶段立即执行被导入模块的顶层副作用，而此刻 <code>main.tsx</code> 自身尚未完成求值，于是形成「main 依赖子模块、子模块又依赖 main」的环。</p><p><code>require()</code> 把求值推迟到运行期：只有当 <code>feature()</code> 在编译期为真、且代码真正运行到该 <code>require</code> 调用时，子模块才被加载。配合 <code>as typeof import(&#39;...&#39;)</code> 的类型断言，编译期依然能拿到完整的类型信息供 TypeScript 校验，运行期则完全规避了环依赖的求值死锁。这是 Bun/Node 混合模块系统下一种典型的「类型静态、求值动态」手法。</p><p>值得对比的是，传统 CommonJS 也能用 <code>require()</code> 规避循环依赖，但缺少静态类型；纯 ESM 则只能靠动态 <code>await import()</code> 规避，而 <code>await</code> 会把调用点变成异步函数、向上传染 <code>async</code>。Claude Code 选用 <code>require()</code> + <code>as typeof import</code> 的组合，在「同步求值」与「静态类型」之间取到了平衡——这种取舍在 51 万行、上千模块的体量下尤为关键。</p><h3 id="_2-2-react-ink-终端-ui" tabindex="-1">2.2 React + Ink 终端 UI <a class="header-anchor" href="#_2-2-react-ink-终端-ui" aria-label="Permalink to &quot;2.2 React + Ink 终端 UI&quot;">​</a></h3><p>终端 UI 基于 <strong>React + Ink</strong>。但 Claude Code 并非直接用上游 Ink，而是在 <code>src/ink/</code> 下维护了一份自研渲染层（含 <code>Ansi.tsx</code>、<code>dom.ts</code>、<code>frame.ts</code>、<code>focus.ts</code>、<code>components/</code>、<code>events/</code> 等）。它本质是一个<strong>终端 React 渲染器</strong>——把 React 组件树渲染成 ANSI 转义序列输出到 stdout，而非浏览器 DOM 或 SSR/SSG。</p><p>最大的 UI 文件是 <code>src/screens/REPL.tsx</code>，单文件 <strong>5,005 行</strong>，承载了主交互界面（输入框、消息列表、权限请求、流式输出、工具调用卡片等几乎所有可见元素的编排）。</p><p>之所以在终端里引入 React，是因为 Claude Code 的 UI 状态高度耦合：流式 token 逐字到达、工具调用卡片需要根据权限状态切换形态、消息列表要支持滚动与历史回看、用户输入与模型输出交错出现。用命令式代码手工管理这些状态会迅速失控；React 的声明式渲染 + Hooks 模型恰好匹配这种「状态多、更新频繁、可组合」的场景。自研 <code>ink/</code> 的存在则说明上游 Ink 不能完全满足需求——Claude Code 对 ANSI 控制序列、焦点管理、双向文本（<code>bidi.ts</code>）、帧布局（<code>frame.ts</code>）有更精细的要求，因此选择分叉维护。</p><h3 id="_2-3-其余核心依赖" tabindex="-1">2.3 其余核心依赖 <a class="header-anchor" href="#_2-3-其余核心依赖" aria-label="Permalink to &quot;2.3 其余核心依赖&quot;">​</a></h3><table tabindex="0"><thead><tr><th>库</th><th>作用</th></tr></thead><tbody><tr><td><code>@commander-js/extra-typings</code></td><td>CLI 参数解析（类型增强版 Commander.js）</td></tr><tr><td><code>zod</code>（v4）</td><td>schema 校验，工具入参/配置/命令参数统一用它约束</td></tr><tr><td><code>lodash-es</code></td><td><code>mapValues</code> / <code>pickBy</code> / <code>uniqBy</code> 等函数式工具</td></tr><tr><td><code>@anthropic-ai/sdk</code></td><td>Anthropic 官方 SDK，封装流式 API</td></tr><tr><td><code>@modelcontextprotocol/sdk</code></td><td>MCP 协议客户端，接入外部工具/资源</td></tr><tr><td><code>chalk</code></td><td>终端着色</td></tr><tr><td>GrowthBook（<code>src/services/analytics/growthbook.ts</code>）</td><td>feature flag / AB 测试，运行期动态下发</td></tr><tr><td>OpenTelemetry + gRPC</td><td>遥测上报</td></tr></tbody></table><p><code>src/main.tsx</code> 顶部的 import 段落即可印证上述选型：Commander、chalk、lodash-es、React、GrowthBook 初始化、API bootstrap、MCP 官方注册表预取都在其中。</p><p>其中几项的作用值得点明：<strong>Zod v4</strong> 是全栈的 schema 守门员——工具入参、命令参数、MCP 配置、权限规则都先经 Zod 校验再进入业务逻辑，这把「运行期类型错误」前移为「解析期失败」，在动态拼接大量工具定义的场景下尤为关键。<strong>GrowthBook</strong> 与编译期 <code>feature()</code> 互补：<code>feature()</code> 决定代码是否被打包，GrowthBook 决定已打包代码在运行期是否启用，后者支持按用户/组织维度做 AB 实验与渐进发布。<strong>OpenTelemetry + gRPC</strong> 则把启动阶段的 <code>profileCheckpoint</code> 打点、工具调用耗时、API 请求延迟统一上报，形成可观测闭环——这也是为什么启动流程里能看到如此密集的 <code>profileCheckpoint</code> 调用，它们最终都汇入遥测管线供性能分析。</p><h2 id="三、源码目录结构" tabindex="-1">三、源码目录结构 <a class="header-anchor" href="#三、源码目录结构" aria-label="Permalink to &quot;三、源码目录结构&quot;">​</a></h2><h3 id="_3-1-顶层组织" tabindex="-1">3.1 顶层组织 <a class="header-anchor" href="#_3-1-顶层组织" aria-label="Permalink to &quot;3.1 顶层组织&quot;">​</a></h3><p><code>src/</code> 下共 53 个顶层条目（35 个目录 + 18 个文件）。按职责可归为九层：</p><table tabindex="0"><thead><tr><th>层</th><th>关键路径</th><th>规模</th><th>职责</th></tr></thead><tbody><tr><td>入口/引导</td><td><code>main.tsx</code>、<code>entrypoints/</code>、<code>setup.ts</code></td><td>main.tsx 4,683 行；setup.ts 477 行</td><td>进程入口、CLI 解析、初始化</td></tr><tr><td>UI/表现</td><td><code>screens/</code>、<code>components/</code>、<code>ink/</code></td><td>REPL.tsx 5,005 行；components/ 144 条目</td><td>React + Ink 终端组件</td></tr><tr><td>业务逻辑</td><td><code>QueryEngine.ts</code>、<code>query.ts</code>、<code>commands.ts</code></td><td>1,295 / 1,729 / 754 行</td><td>查询引擎、流式循环、命令注册</td></tr><tr><td>工具系统</td><td><code>Tool.ts</code>、<code>tools.ts</code>、<code>tools/</code></td><td>Tool.ts 792 行；tools.ts 389 行；tools/ 43 条目</td><td>工具接口、注册、~40 个工具</td></tr><tr><td>服务层</td><td><code>services/</code></td><td>36 条目（20 子目录 + 16 文件）</td><td>API、MCP、LSP、Compact、Analytics 等</td></tr><tr><td>状态</td><td><code>state/</code></td><td>6 文件</td><td>AppState、store、selectors</td></tr><tr><td>Hooks</td><td><code>hooks/</code></td><td>104 文件（85 顶层条目）</td><td>React Hook 驱动的业务逻辑</td></tr><tr><td>IDE 桥接</td><td><code>bridge/</code></td><td>31 文件</td><td>JSON-RPC over WebSocket，IDE 扩展</td></tr><tr><td>记忆/任务</td><td><code>memdir/</code>、<code>tasks/</code></td><td>tasks/ 9 条目</td><td>持久化记忆、后台任务</td></tr></tbody></table><h3 id="_3-2-各层要点" tabindex="-1">3.2 各层要点 <a class="header-anchor" href="#_3-2-各层要点" aria-label="Permalink to &quot;3.2 各层要点&quot;">​</a></h3><p><strong>入口/引导层</strong>：<code>entrypoints/</code> 包含 <code>cli.tsx</code>（CLI 入口）、<code>init.ts</code>（初始化）、<code>mcp.ts</code>（MCP server 模式入口）、<code>agentSdkTypes.ts</code>、<code>sandboxTypes.ts</code> 以及 <code>sdk/</code> 子目录。<code>setup.ts</code>（477 行）负责首启环境检测与补全。</p><p><strong>UI/表现层</strong>：<code>screens/</code> 只有三个文件——<code>REPL.tsx</code>（主界面）、<code>Doctor.tsx</code>（诊断）、<code>ResumeConversation.tsx</code>（会话恢复）。<code>components/</code> 含 144 个条目，是组件库。<code>ink/</code> 是自研的终端 React 渲染器。</p><p><strong>业务逻辑层</strong>：三个大文件构成核心。<code>QueryEngine.ts</code>（1,295 行）封装「构造请求—流式接收—工具调度」的引擎；<code>query.ts</code>（1,729 行）是更底层的流式查询实现；<code>commands.ts</code>（754 行）负责斜杠命令注册。约 70 个命令、约 40 个工具都挂在这里被调度。</p><p><strong>工具系统</strong>：<code>Tool.ts</code>（792 行）定义统一的 <code>Tool</code> 接口与生命周期；<code>tools.ts</code>（389 行）的 <code>getTools()</code> 聚合所有内置工具；<code>tools/</code> 下 43 个条目（42 个工具子目录 + 索引文件）每个对应一个工具实现。</p><p><strong>服务层</strong>：<code>services/</code> 共 36 个条目，按功能域拆分为 API（<code>api/</code>）、MCP（<code>mcp/</code>）、LSP（<code>lsp/</code>）、Compact（压缩）、Analytics（<code>analytics/</code> 含 GrowthBook）、OAuth、文件等 20 个子目录。服务层是「无状态能力提供者」，被业务逻辑层调用。</p><p><strong>状态层</strong>：<code>state/</code> 仅 6 个文件：<code>AppState.tsx</code>、<code>AppStateStore.ts</code>、<code>store.ts</code>、<code>selectors.ts</code>、<code>onChangeAppState.ts</code>、<code>teammateViewHelpers.ts</code>。Claude Code 没有引入 Zustand/Redux，而是自研了一个基于 React Context 的轻量 store。</p><p><strong>Hooks 层</strong>：<code>hooks/</code> 共 104 个文件（85 个顶层条目），是 Claude Code 的一大特色——大量业务逻辑以 React Hook 形式实现，挂在组件树里随生命周期运行，而非放在独立的服务对象中。</p><p><strong>桥接层</strong>：<code>bridge/</code> 31 个文件，用于 IDE 扩展（VS Code 等）通信，基于 JSON-RPC over WebSocket，也支撑远程会话与 SDK 适配。</p><p><strong>记忆/任务层</strong>：<code>memdir/</code> 提供跨会话持久化记忆；<code>tasks/</code> 9 个条目对应 6 种后台任务（<code>DreamTask</code>、<code>InProcessTeammateTask</code>、<code>LocalAgentTask</code>、<code>LocalMainSessionTask</code>、<code>LocalShellTask</code>、<code>RemoteAgentTask</code>）加 3 个辅助文件，是后台异步执行的基础设施。</p><h3 id="_3-3-目录组织的设计取向" tabindex="-1">3.3 目录组织的设计取向 <a class="header-anchor" href="#_3-3-目录组织的设计取向" aria-label="Permalink to &quot;3.3 目录组织的设计取向&quot;">​</a></h3><p>从目录结构可以读出 Claude Code 的两个组织取向。</p><p>其一，<strong>逻辑与状态分离</strong>。业务逻辑大量沉淀在 <code>hooks/</code>（104 个文件）而非 <code>services/</code> 中——服务层保持无状态、只提供能力，真正的业务流程（何时调用何种服务、如何串联工具与权限）由 Hook 在组件树内编排。这种「Hook 即业务」的风格让逻辑天然随 UI 生命周期运行，也意味着理解任何一个功能都需要在 <code>hooks/</code> 与对应组件之间来回跳转。</p><p>其二，<strong>工具与命令的一等公民地位</strong>。<code>tools/</code> 下每个工具独占一个子目录（如 <code>tools/BashTool/</code>、<code>tools/FileEditTool/</code>），内部包含该工具的提示词、Schema、实现与权限描述；<code>commands/</code> 下每个斜杠命令同样自成模块。这种「一工具一目录」的布局便于隔离变更、独立测试，也使得工具数量从几个增长到四十余个时，目录结构依然扁平可读。</p><p>相比之下，<code>state/</code> 只有 6 个文件——状态管理刻意保持轻量，全局状态收敛在 <code>AppStateStore.ts</code>，派生状态由 <code>selectors.ts</code> 计算，跨组件同步靠 <code>onChangeAppState.ts</code> 的监听。没有引入 Redux/Zustand 这类外部状态库，说明 Claude Code 的共享状态规模可控，主要矛盾不在状态而在流程编排。</p><h2 id="四、启动流程" tabindex="-1">四、启动流程 <a class="header-anchor" href="#四、启动流程" aria-label="Permalink to &quot;四、启动流程&quot;">​</a></h2><p>启动流程是理解 Claude Code 的关键。它不是线性的「加载—渲染」，而是一段精心编排的<strong>并行预热 + 分阶段初始化</strong>。</p><h3 id="_4-1-预热阶段-与模块导入抢时间" tabindex="-1">4.1 预热阶段：与模块导入抢时间 <a class="header-anchor" href="#_4-1-预热阶段-与模块导入抢时间" aria-label="Permalink to &quot;4.1 预热阶段：与模块导入抢时间&quot;">​</a></h3><p><code>src/main.tsx</code> 开头的注释直接点明了设计意图（<code>src/main.tsx:1-8</code>）：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>// These side-effects must run before all other imports:</span></span>
<span class="line"><span>// 1. profileCheckpoint marks entry before heavy module evaluation begins</span></span>
<span class="line"><span>// 2. startMdmRawRead fires MDM subprocesses (plutil/reg query) so they run in</span></span>
<span class="line"><span>//    parallel with the remaining ~135ms of imports below</span></span>
<span class="line"><span>// 3. startKeychainPrefetch fires both macOS keychain reads (OAuth + legacy API</span></span>
<span class="line"><span>//    key) in parallel — isRemoteManagedSettingsEligible() otherwise reads them</span></span>
<span class="line"><span>//    sequentially via sync spawn inside applySafeConfigEnvironmentVariables()</span></span>
<span class="line"><span>//    (~65ms on every macOS startup)</span></span></code></pre></div><p>对应代码：</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { profileCheckpoint, profileReport } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;./utils/startupProfiler.js&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">profileCheckpoint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;main_tsx_entry&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);                              </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// src/main.tsx:12</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { startMdmRawRead } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;./utils/settings/mdm/rawRead.js&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">startMdmRawRead</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();                                                  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// src/main.tsx:16</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { ensureKeychainPrefetchCompleted, startKeychainPrefetch } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;./utils/secureStorage/keychainPrefetch.js&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">startKeychainPrefetch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();                                            </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// src/main.tsx:20</span></span></code></pre></div><p>关键点：</p><ol><li><strong><code>profileCheckpoint</code></strong> 贯穿整个启动过程，是一个轻量的时间戳打点器（<code>src/utils/startupProfiler.js</code>），用于事后还原启动耗时。</li><li><strong><code>startMdmRawRead()</code></strong> 触发 MDM（移动设备管理）配置读取子进程（macOS 用 <code>plutil</code>、Windows 用 <code>reg query</code>），让它们在后台与后续约 <strong>135ms</strong> 的模块导入并行跑。</li><li><strong><code>startKeychainPrefetch()</code></strong> 并行预取 macOS 钥匙串中的 OAuth token 与 API key——否则这些读取会在配置加载阶段被同步串行调用，每次启动多耗约 <strong>65ms</strong>。</li></ol><p>这三个副作用在所有 <code>import</code> 之前执行，把「IO 等待」与「CPU 导入」重叠起来。等到模块导入完成时，钥匙串和 MDM 数据通常已就绪：</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">profileCheckpoint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;main_tsx_imports_loaded&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);                       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// src/main.tsx:209</span></span></code></pre></div><h3 id="_4-2-配置与认证" tabindex="-1">4.2 配置与认证 <a class="header-anchor" href="#_4-2-配置与认证" aria-label="Permalink to &quot;4.2 配置与认证&quot;">​</a></h3><p>模块就绪后进入 <code>main()</code> 函数（<code>profileCheckpoint(&#39;main_function_start&#39;)</code>，<code>src/main.tsx:586</code>），随后：</p><ul><li><strong>配置加载</strong>：<code>eagerLoadSettings</code>（<code>src/main.tsx:502-516</code>）读取全局配置、项目配置、MCP 配置；<code>ensureKeychainPrefetchCompleted()</code> 在此处 await 预取结果。</li><li><strong>认证</strong>：判定客户端类型（API key 直连 vs OAuth token），<code>profileCheckpoint(&#39;main_client_type_determined&#39;)</code>（<code>src/main.tsx:849</code>）标记完成。</li><li><strong>Feature flag</strong>：<code>initializeGrowthBook()</code>（来自 <code>src/services/analytics/growthbook.ts</code>）拉取 GrowthBook 远端配置，决定哪些 <code>feature()</code> 在运行期为真。</li></ul><h3 id="_4-3-工具、插件、mcp、技能注册" tabindex="-1">4.3 工具、插件、MCP、技能注册 <a class="header-anchor" href="#_4-3-工具、插件、mcp、技能注册" aria-label="Permalink to &quot;4.3 工具、插件、MCP、技能注册&quot;">​</a></h3><p>进入 Commander action handler 后，依次：</p><ol><li><p><strong>工具注册</strong>：<code>let tools = getTools(toolPermissionContext)</code>（<code>src/main.tsx:1868</code>），加载全部约 40 个内置工具。注意此处在调用前已初始化 <code>toolPermissionContext</code>，使工具能感知当前权限模式——这决定了每个工具的 <code>isEnabled()</code> 在不同模式下返回不同结果，例如 <code>plan</code> 模式下写工具会被禁用。注册完成后立即 <code>profileCheckpoint(&#39;action_tools_loaded&#39;)</code>（<code>src/main.tsx:1878</code>）打点。</p></li><li><p><strong>插件与技能</strong>：<code>initBuiltinPlugins()</code>（<code>src/main.tsx:1924</code>）与 <code>initBundledSkills()</code>（<code>src/main.tsx:1925</code>）紧邻执行——内置插件与打包技能在同一阶段就绪。源码注释（<code>src/main.tsx:1918-1922</code>）说明提前到此处的原因是<strong>时序</strong>而非依赖：这两个调用是「纯内存数组 push（&lt;1ms，零 IO）」，但如果延后到 <code>setup()</code> 内部执行，<code>setup()</code> 里有约 20ms 的 await 点，与它并行的 <code>getCommands()</code> 会在这段 await 间隙记忆到一个空列表。注意此处两调用被 <code>if (process.env.CLAUDE_CODE_ENTRYPOINT !== &#39;local-agent&#39;)</code> 守卫包裹，即 <code>local-agent</code> 入口路径下跳过。此处还可能把 <code>SyntheticOutputTool</code> 追加进工具数组（<code>src/main.tsx:1888</code> 注释说明它在 <code>getTools()</code> 过滤之后单独添加），用于合成特定的输出行为。</p></li><li><p><strong>MCP 连接预取</strong>：<code>prefetchAllMcpResources(regularMcpConfigs)</code>（<code>src/main.tsx:2412</code>）并行连接所有配置的 MCP server，预取它们提供的工具/命令/资源。此处有去重逻辑：源码注释（<code>src/main.tsx:2422</code>）指出「Merge with dedup by name」——同一名称的 MCP 资源在不同配置来源下会被合并去重，避免工具列表里出现重复项。MCP 连接的耗时取决于远端 server 响应，是启动阶段最不可控的一段，因此被设计为并行预取而非串行等待。</p></li></ol><p>这三步构成「能力装配」阶段：工具是本地能力、插件是扩展能力、MCP 是远端能力，三者在进入主循环前必须全部就绪，否则用户首条输入可能触发未注册工具的调用。</p><h3 id="_4-4-ui-渲染与主循环入口" tabindex="-1">4.4 UI 渲染与主循环入口 <a class="header-anchor" href="#_4-4-ui-渲染与主循环入口" aria-label="Permalink to &quot;4.4 UI 渲染与主循环入口&quot;">​</a></h3><p>最后启动 Ink 渲染器、挂载 REPL 组件，并调用：</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> launchRepl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(root, appProps, replProps, renderAndRun);          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// src/main.tsx:3134</span></span></code></pre></div><p><code>launchRepl</code>（来自 <code>src/replLauncher.tsx</code>）是整个交互主循环的入口，此后控制权移交 continuation-driven 的查询引擎。仓库内 <code>launchRepl</code> 共有三处调用（<code>src/main.tsx:3134</code>、<code>3176</code>、<code>3242</code>），分别对应正常交互、特定会话恢复、特殊入口三种启动路径。</p><h3 id="_4-4-1-为何把-io-预热藏进导入时间" tabindex="-1">4.4.1 为何把 IO 预热藏进导入时间 <a class="header-anchor" href="#_4-4-1-为何把-io-预热藏进导入时间" aria-label="Permalink to &quot;4.4.1 为何把 IO 预热藏进导入时间&quot;">​</a></h3><p><code>profileCheckpoint</code> 的打点数据揭示了启动优化的核心矛盾：模块导入是 CPU 密集且不可压缩的（Bun 仍需解析、求值上千个模块），而钥匙串读取与 MDM 查询是 IO 等待、天然可并行。如果把这两项留到导入完成后再串行执行，每次 macOS 启动会额外多耗约 65ms（钥匙串）加上 MDM 子进程时间，而这段等待里 CPU 是空闲的。</p><p>把 IO 预热提到所有 <code>import</code> 之前、以「发射即忘」的方式启动子进程与钥匙串读取，等于把 IO 等待「塞进」CPU 导入的间隙。等到 <code>eagerLoadSettings</code> 真正需要这些数据时，它们大概率已就绪，只需 <code>ensureKeychainPrefetchCompleted()</code> 做一次轻量 await 即可。这是用「并行」换「串行」的典型启动优化，代价只是代码可读性——顶层副作用在多数代码规范里是反模式，因此源码专门用 <code>// eslint-disable-next-line custom-rules/no-top-level-side-effects</code> 逐行豁免，并配顶部注释说明三步的意图。</p><h3 id="_4-4-2-continuation-driven-polling-的伏笔" tabindex="-1">4.4.2 continuation-driven polling 的伏笔 <a class="header-anchor" href="#_4-4-2-continuation-driven-polling-的伏笔" aria-label="Permalink to &quot;4.4.2 continuation-driven polling 的伏笔&quot;">​</a></h3><p>注意 <code>launchRepl</code> 之后整个主循环并不由 <code>main.tsx</code> 驱动，而是由 <code>QueryEngine</code> 在每一轮 API 响应返回后决定是否构造下一轮。<code>main.tsx</code> 在 <code>launchRepl</code> 处把控制权完全交出，自身只负责「把环境准备好、把 REPL 挂上去」。这种「入口即交接点」的设计，与 OpenCode 在主函数里用显式循环驱动、Codex 在 Tokio runtime 里 spawn 任务的风格形成鲜明对照——也是第 2 篇将要展开的 continuation-driven polling 架构在启动阶段的伏笔。</p><h3 id="_4-5-启动时序图" tabindex="-1">4.5 启动时序图 <a class="header-anchor" href="#_4-5-启动时序图" aria-label="Permalink to &quot;4.5 启动时序图&quot;">​</a></h3><p>下图把上述阶段按「并行 / 串行」关系展开：</p>`,85)),(o(),c(r,null,{default:t(()=>[s(a,{id:"mermaid-698",class:"mermaid",graph:"flowchart%20TD%0A%20%20%20%20A%5B%22profileCheckpoint('main_tsx_entry')%3Cbr%2F%3Esrc%2Fmain.tsx%3A12%22%5D%20--%3E%20B%7B%E5%B9%B6%E8%A1%8C%E9%A2%84%E7%83%AD%E9%98%B6%E6%AE%B5%7D%0A%20%20%20%20B%20--%3E%20C1%5B%22startMdmRawRead()%3Cbr%2F%3EMDM%20%E5%AD%90%E8%BF%9B%E7%A8%8B%3Cbr%2F%3Esrc%2Fmain.tsx%3A16%22%5D%0A%20%20%20%20B%20--%3E%20C2%5B%22startKeychainPrefetch()%3Cbr%2F%3EOAuth%20%2B%20API%20key%3Cbr%2F%3Esrc%2Fmain.tsx%3A20%22%5D%0A%20%20%20%20B%20--%3E%20C3%5B%22%E6%A8%A1%E5%9D%97%E5%AF%BC%E5%85%A5%20~135ms%3Cbr%2F%3E~200%20%E4%B8%AA%20import%22%5D%0A%20%20%20%20C1%20--%3E%20D%5B%22profileCheckpoint('main_tsx_imports_loaded')%3Cbr%2F%3Esrc%2Fmain.tsx%3A209%22%5D%0A%20%20%20%20C2%20--%3E%20D%0A%20%20%20%20C3%20--%3E%20D%0A%20%20%20%20D%20--%3E%20E%5B%22main()%20%E5%90%AF%E5%8A%A8%3Cbr%2F%3Esrc%2Fmain.tsx%3A586%22%5D%0A%20%20%20%20E%20--%3E%20F%5B%22%E9%85%8D%E7%BD%AE%E5%8A%A0%E8%BD%BD%3Cbr%2F%3EeagerLoadSettings%20%2B%20ensureKeychainPrefetchCompleted%22%5D%0A%20%20%20%20F%20--%3E%20G%5B%22%E8%AE%A4%E8%AF%81%E5%88%A4%E5%AE%9A%3Cbr%2F%3EAPI%20key%20%2F%20OAuth%22%5D%0A%20%20%20%20G%20--%3E%20H%5B%22GrowthBook%20%E5%88%9D%E5%A7%8B%E5%8C%96%3Cbr%2F%3Efeature%20flag%20%E4%B8%8B%E5%8F%91%22%5D%0A%20%20%20%20H%20--%3E%20I%5B%22getTools()%3Cbr%2F%3E~40%20%E5%B7%A5%E5%85%B7%3Cbr%2F%3Esrc%2Fmain.tsx%3A1868%22%5D%0A%20%20%20%20I%20--%3E%20J%5B%22initBuiltinPlugins()%3Cbr%2F%3EinitBundledSkills()%3Cbr%2F%3Esrc%2Fmain.tsx%3A1924-1925%22%5D%0A%20%20%20%20J%20--%3E%20K%5B%22prefetchAllMcpResources()%3Cbr%2F%3Esrc%2Fmain.tsx%3A2412%22%5D%0A%20%20%20%20K%20--%3E%20L%5B%22Ink%20%E6%B8%B2%E6%9F%93%E5%99%A8%E5%90%AF%E5%8A%A8%3Cbr%2F%3EREPL%20%E7%BB%84%E4%BB%B6%E6%8C%82%E8%BD%BD%22%5D%0A%20%20%20%20L%20--%3E%20M%5B%22launchRepl()%3Cbr%2F%3E%E4%B8%BB%E5%BE%AA%E7%8E%AF%E5%85%A5%E5%8F%A3%3Cbr%2F%3Esrc%2Fmain.tsx%3A3134%22%5D%0A"})]),fallback:t(()=>[...e[0]||(e[0]=[n(" Loading... ",-1)])]),_:1})),e[3]||(e[3]=d('<p>上图清晰展示了 Claude Code 启动的两个关键设计：<strong>顶部三路并行预热</strong>把 IO 等待藏进了模块导入时间；<strong>中段严格串行</strong>保证配置→认证→特性→工具→MCP 的依赖顺序。<code>profileCheckpoint</code> 在每个关键节点打点，使整条链路可观测、可优化。</p><h2 id="五、分层架构" tabindex="-1">五、分层架构 <a class="header-anchor" href="#五、分层架构" aria-label="Permalink to &quot;五、分层架构&quot;">​</a></h2><h3 id="_5-1-四层划分" tabindex="-1">5.1 四层划分 <a class="header-anchor" href="#_5-1-四层划分" aria-label="Permalink to &quot;5.1 四层划分&quot;">​</a></h3><p>把 <code>src/</code> 的目录组织抽象化，Claude Code 是一个清晰的四层架构：</p><p>这种四层划分的依据是<strong>依赖方向</strong>而非功能域：上层依赖下层，下层不反向依赖上层。表现层依赖业务逻辑层提供的 QueryEngine 与命令，业务逻辑层依赖服务层提供的外部能力，服务层依赖基础设施层的状态与工具函数。划分的边界与目录边界基本对齐——<code>screens/</code>+<code>components/</code>+<code>ink/</code> 属表现层，<code>QueryEngine.ts</code>+<code>commands.ts</code>+<code>tools/</code> 属业务层，<code>services/</code> 属服务层，<code>utils/</code>+<code>state/</code>+<code>hooks/</code> 属基础设施层。依赖方向的一致性是这套架构能支撑 51 万行规模而不崩塌的根本原因。</p><table tabindex="0"><thead><tr><th>层</th><th>组成</th><th>职责</th></tr></thead><tbody><tr><td>表现层 Presentation</td><td><code>screens/</code>、<code>components/</code>、<code>ink/</code></td><td>React 组件 + Ink 渲染器，负责终端交互与可视化</td></tr><tr><td>业务逻辑层 Business Logic</td><td><code>QueryEngine.ts</code>、<code>query.ts</code>、<code>commands.ts</code>、<code>Tool.ts</code>、<code>tools/</code></td><td>查询引擎、输入预处理、~70 命令、~40 工具</td></tr><tr><td>服务层 Services</td><td><code>services/</code> 下 API、MCP、LSP、Auth、Analytics、Compact 等</td><td>无状态能力提供者，封装外部交互</td></tr><tr><td>基础设施层 Infrastructure</td><td><code>utils/</code>、<code>state/</code>、<code>hooks/</code>、<code>memdir/</code>、权限系统、遥测</td><td>文件系统、权限、状态、错误处理、OTel</td></tr></tbody></table><h3 id="_5-1-1-九层与四层的映射" tabindex="-1">5.1.1 九层与四层的映射 <a class="header-anchor" href="#_5-1-1-九层与四层的映射" aria-label="Permalink to &quot;5.1.1 九层与四层的映射&quot;">​</a></h3><p>前文 §3.1 的「九层」是按目录粒度展开的物理组织视图，「四层」是按依赖方向归并后的逻辑视图。两者并非并列，而是细化与归并的关系。下表显式对齐每一层：</p><table tabindex="0"><thead><tr><th>九层（物理）</th><th>四层（逻辑）</th><th>说明</th></tr></thead><tbody><tr><td>入口/引导层（<code>main.tsx</code>、<code>entrypoints/</code>、<code>setup.ts</code>）</td><td>基础设施层</td><td>引导代码不属表现/业务/服务，作为启动期基础设施下沉</td></tr><tr><td>UI/表现层（<code>screens/</code>、<code>components/</code>、<code>ink/</code>）</td><td>表现层</td><td>一一对应</td></tr><tr><td>业务逻辑层（<code>QueryEngine.ts</code>、<code>query.ts</code>、<code>commands.ts</code>）</td><td>业务逻辑层</td><td>一一对应</td></tr><tr><td>工具系统（<code>Tool.ts</code>、<code>tools.ts</code>、<code>tools/</code>）</td><td>业务逻辑层</td><td>工具是业务逻辑的执行单元，归并</td></tr><tr><td>服务层（<code>services/</code>）</td><td>服务层</td><td>一一对应</td></tr><tr><td>状态（<code>state/</code>）</td><td>基础设施层</td><td>全局状态下沉为基础设施</td></tr><tr><td>Hooks（<code>hooks/</code>）</td><td>基础设施层（横切至表现层）</td><td>Hook 是横切三层的胶水，物理上归基础设施</td></tr><tr><td>IDE 桥接（<code>bridge/</code>）</td><td>基础设施层</td><td>远程/SDK 通信作为基础设施下沉</td></tr><tr><td>记忆/任务（<code>memdir/</code>、<code>tasks/</code>）</td><td>基础设施层</td><td>持久化与后台任务下沉</td></tr></tbody></table><p>需要说明两点越界：其一，<code>hooks/</code> 物理上属基础设施层，但运行期绑定在组件树上、横跨至表现层甚至调用业务逻辑与服务层，是事实上的跨层胶水（详见 §5.4）；其二，<code>bridge/</code> 与 <code>tasks/</code> 在九层中单列，归并到四层后属基础设施层，不再单列。其余未纳入九层的目录（如 <code>coordinator/</code>、<code>assistant/</code>、<code>voice/</code>、<code>vim/</code>、<code>buddy/</code>、<code>migrations/</code>、<code>native-ts/</code>、<code>plugins/</code>、<code>skills/</code>、<code>constants/</code>、<code>schemas/</code>、<code>types/</code>、<code>context/</code>、<code>keybindings/</code>、<code>cli/</code>、<code>remote/</code>、<code>server/</code> 等）多为 feature-flag 门控的实验性模块、构建迁移工具或配置常量，规模较小或尚未稳定，不在核心架构骨架内，待相关章节展开时单独说明。</p><h3 id="_5-2-架构示意" tabindex="-1">5.2 架构示意 <a class="header-anchor" href="#_5-2-架构示意" aria-label="Permalink to &quot;5.2 架构示意&quot;">​</a></h3>',11)),(o(),c(r,null,{default:t(()=>[s(a,{id:"mermaid-903",class:"mermaid",graph:"flowchart%20TB%0A%20%20%20%20subgraph%20Presentation%5B%E8%A1%A8%E7%8E%B0%E5%B1%82%20Presentation%5D%0A%20%20%20%20%20%20%20%20P1%5B%22REPL.tsx%205%2C005%20%E8%A1%8C%22%5D%0A%20%20%20%20%20%20%20%20P2%5B%22PromptInput%22%5D%0A%20%20%20%20%20%20%20%20P3%5B%22MessageList%22%5D%0A%20%20%20%20%20%20%20%20P4%5B%22PermissionRequest%22%5D%0A%20%20%20%20%20%20%20%20P5%5B%22%E8%87%AA%E7%A0%94%20Ink%20%E6%B8%B2%E6%9F%93%E5%99%A8%20ink%2F%22%5D%0A%20%20%20%20end%0A%20%20%20%20subgraph%20Business%5B%E4%B8%9A%E5%8A%A1%E9%80%BB%E8%BE%91%E5%B1%82%20Business%20Logic%5D%0A%20%20%20%20%20%20%20%20B1%5B%22QueryEngine%201%2C295%20%E8%A1%8C%22%5D%0A%20%20%20%20%20%20%20%20B2%5B%22query.ts%201%2C729%20%E8%A1%8C%22%5D%0A%20%20%20%20%20%20%20%20B3%5B%22Commands%20~50%22%5D%0A%20%20%20%20%20%20%20%20B4%5B%22Tools%20~40%22%5D%0A%20%20%20%20end%0A%20%20%20%20subgraph%20Services%5B%E6%9C%8D%E5%8A%A1%E5%B1%82%20Services%5D%0A%20%20%20%20%20%20%20%20S1%5B%22API%20Service%22%5D%0A%20%20%20%20%20%20%20%20S2%5B%22MCP%20Service%22%5D%0A%20%20%20%20%20%20%20%20S3%5B%22LSP%20Service%22%5D%0A%20%20%20%20%20%20%20%20S4%5B%22Auth%20Service%22%5D%0A%20%20%20%20%20%20%20%20S5%5B%22Analytics%20%2F%20GrowthBook%22%5D%0A%20%20%20%20%20%20%20%20S6%5B%22Compact%20Service%22%5D%0A%20%20%20%20end%0A%20%20%20%20subgraph%20Infra%5B%E5%9F%BA%E7%A1%80%E8%AE%BE%E6%96%BD%E5%B1%82%20Infrastructure%5D%0A%20%20%20%20%20%20%20%20I1%5B%22%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F%20utils%22%5D%0A%20%20%20%20%20%20%20%20I2%5B%22%E6%9D%83%E9%99%90%E7%B3%BB%E7%BB%9F%207%20%E6%A8%A1%E5%BC%8F%22%5D%0A%20%20%20%20%20%20%20%20I3%5B%22%E7%8A%B6%E6%80%81%E7%AE%A1%E7%90%86%20state%2F%22%5D%0A%20%20%20%20%20%20%20%20I4%5B%22%E9%94%99%E8%AF%AF%E5%A4%84%E7%90%86%22%5D%0A%20%20%20%20%20%20%20%20I5%5B%22%E9%81%A5%E6%B5%8B%20OpenTelemetry%22%5D%0A%20%20%20%20end%0A%20%20%20%20Presentation%20--%3E%20Business%0A%20%20%20%20Business%20--%3E%20Services%0A%20%20%20%20Services%20--%3E%20Infra%0A"})]),fallback:t(()=>[...e[1]||(e[1]=[n(" Loading... ",-1)])]),_:1})),e[4]||(e[4]=d('<h3 id="_5-3-各层职责说明" tabindex="-1">5.3 各层职责说明 <a class="header-anchor" href="#_5-3-各层职责说明" aria-label="Permalink to &quot;5.3 各层职责说明&quot;">​</a></h3><p><strong>表现层</strong>：核心是 <code>REPL.tsx</code>，它把输入框（<code>PromptInput</code>）、消息列表（<code>MessageList</code>）、权限请求弹层（<code>PermissionRequest</code>）与流式输出组织成一棵组件树，交给自研 <code>ink/</code> 渲染器输出 ANSI。表现层只关心「怎么显示」，不直接发起 API 调用。</p><p><strong>业务逻辑层</strong>：<code>QueryEngine</code> 是心脏——它接收已预处理好的用户输入，构造 Anthropic API 请求，接收流式响应，遇到 <code>tool_use</code> 块时交由工具系统执行，再把 <code>tool_result</code> 回填进入下一轮。<code>commands.ts</code> 注册约 70 个斜杠命令（<code>/commit</code>、<code>/review</code>、<code>/mcp</code>、<code>/memory</code> 等），<code>Tool.ts</code> 定义统一工具接口。</p><p><strong>服务层</strong>：是「能力提供者」，不含业务状态。API Service 封装 Anthropic SDK 调用，MCP Service 管理外部工具连接，LSP Service 接入语言服务器，Compact Service 负责对话压缩（本系列第 4 篇主题），Analytics + GrowthBook 负责遥测与特性开关。</p><p><strong>基础设施层</strong>：权限系统是其中最有特色的部分——它共有 <strong>7 种权限模式</strong>（见 <code>src/types/permissions.ts:16-28</code>）：5 个外部模式 <code>acceptEdits</code> / <code>bypassPermissions</code> / <code>default</code> / <code>dontAsk</code> / <code>plan</code>，加 2 个内部模式 <code>auto</code> / <code>bubble</code>。用户按 Shift+Tab 循环的实际路径是 <code>default → acceptEdits → plan → bypassPermissions → (auto 若可用 else default)</code>（见 <code>src/utils/permissions/getNextPermissionMode.ts:39-77</code>）；其中 <code>auto</code> 模式由 AI 转录分类器驱动、被 <code>TRANSCRIPT_CLASSIFIER</code> feature flag 门控（<code>src/utils/permissions/autoModeState.ts:2</code> 注释明确说明「conditionally require() it on feature(&#39;TRANSCRIPT_CLASSIFIER&#39;)」），<code>dontAsk</code> 与 <code>bubble</code> 是尚未在 UI 暴露的内部模式。状态管理依赖 <code>state/</code> 下的轻量自研 store，配合 <code>hooks/</code> 的 104 个 Hook 把逻辑织入组件树。</p><h3 id="_5-4-层与层之间的真实调用关系" tabindex="-1">5.4 层与层之间的真实调用关系 <a class="header-anchor" href="#_5-4-层与层之间的真实调用关系" aria-label="Permalink to &quot;5.4 层与层之间的真实调用关系&quot;">​</a></h3><p>分层图给出的「表现→业务→服务→基础设施」是理想化的单向依赖，实际代码中存在几处值得注意的「越界」：</p><ul><li><strong>表现层直达服务层</strong>：<code>REPL.tsx</code> 等组件在渲染流式输出时，会直接调用 Analytics、Compact 等服务做埋点与压缩触发，绕过业务逻辑层。这是因为埋点与压缩是渲染的副作用，不改变业务流程。</li><li><strong>Hook 横跨多层</strong>：<code>hooks/</code> 下的 Hook 既是表现层的一部分（绑定在组件上），又承载业务逻辑（决定何时调 QueryEngine）、还会调用服务层能力。Hook 是事实上打通三层的「胶水」，理解它的双向位置是读懂 Claude Code 的关键。</li><li><strong>权限系统被多层共享</strong>：业务逻辑层在执行工具前查权限，服务层在调 MCP 前查权限，连表现层在渲染权限请求弹层时也要读当前模式。权限不是一个「层」，而是一条贯穿各层的横切关注点（cross-cutting concern），这也是它被放在 <code>utils/permissions/</code> 而非 <code>services/</code> 的原因。</li></ul><p>把握这三点越界关系，就能解释为什么 <code>REPL.tsx</code> 会膨胀到 5,005 行——它不只是表现层组件，还承担了大量本应下沉的编排职责。这种「大组件」是 Claude Code 架构上可改进的地方，也是后续版本拆分的重点。</p><h2 id="六、用户请求生命周期预览" tabindex="-1">六、用户请求生命周期预览 <a class="header-anchor" href="#六、用户请求生命周期预览" aria-label="Permalink to &quot;六、用户请求生命周期预览&quot;">​</a></h2><p>本节简要勾勒一次完整请求的流转，作为后续各章的索引。详细拆解留待第 2 篇主循环章。</p><ol><li><strong>用户输入</strong>：在终端 <code>PromptInput</code> 中键入文本并回车。</li><li><strong>输入预处理</strong>：<code>processUserInput.ts</code> 解析斜杠命令、补全上下文标记、判断是否需要走命令路径而非模型路径。</li><li><strong>上下文准备</strong>：<code>context.ts</code> 汇集系统提示、历史消息、工具定义、记忆文件（claude.md / memdir），构造 API 请求体。</li><li><strong>QueryEngine 调用</strong>：<code>QueryEngine.ts</code> 发起流式 API 请求，逐 token 接收响应。</li><li><strong>工具执行 + 权限检查</strong>：响应中出现 <code>tool_use</code> 时，按当前权限模式决定是否放行；放行后在本地执行工具（读文件、运行 bash、调 MCP 等）。</li><li><strong>结果处理</strong>：工具输出封装为 <code>tool_result</code>，回填进消息列表。</li><li><strong>循环或终止</strong>：若有待执行工具，回到第 4 步继续下一轮流式调用；若模型返回 <code>message_stop</code>，则渲染最终响应，等待下一次用户输入。</li></ol><p>这条链路的关键在于它是 <strong>continuation-driven</strong> 的——循环体本身是 <code>query.ts:307</code> 的一个 <code>while(true)</code>，但它的分支由 API 返回的 <code>stop_reason</code> 与待执行工具列表决定（而非固定步骤序列），每一轮流式调用结束后构造新的 <code>State</code> 再 <code>continue</code> 回循环顶。第 2 篇将深入剖析这套 continuation 机制。</p><p>逐展开七个步骤，能更清楚地看到每一步在分层架构中的落点：</p><ol><li><p><strong>用户输入</strong>：<code>PromptInput</code> 组件捕获回车事件，把原始文本交给预处理层。这一步纯表现层，不含业务判断。</p></li><li><p><strong>输入预处理</strong>：<code>processUserInput.ts</code> 首先做<strong>命令分流</strong>——若输入以 <code>/</code> 开头，解析为斜杠命令（如 <code>/commit</code>、<code>/clear</code>），直接路由到 <code>commands.ts</code> 注册的处理器，不进入模型调用路径。只有非命令输入才进入后续的模型流程。这一步同时完成 <code>@</code> 文件引用展开、<code>#</code> 记忆引用注入等上下文标记的预处理。</p></li><li><p><strong>上下文准备</strong>：<code>context.ts</code> 汇集四类信息——系统提示词（含工具使用规范、环境信息）、历史消息（已被压缩服务裁剪过）、工具定义（当前权限模式下可见的工具子集）、记忆文件（claude.md 与 memdir 持久化内容）。这一步决定模型「能看到什么」，是上下文工程的核心，也是第 10 篇的主题。</p></li><li><p><strong>QueryEngine 调用</strong>：<code>QueryEngine.ts</code> 把上述上下文打包成 Anthropic API 请求，发起<strong>流式</strong>调用。响应以 SSE 方式逐 token 到达，每收到一个 token 就触发渲染更新，让用户看到「打字机」效果。</p></li><li><p><strong>工具执行 + 权限检查</strong>：当流式响应中出现完整的 <code>tool_use</code> 块，<code>QueryEngine</code> 暂停该轮接收，按当前权限模式（用户可见循环为 <code>default</code> / <code>acceptEdits</code> / <code>plan</code> / <code>bypassPermissions</code>，<code>auto</code> 由 AI 分类器插入）决定是否需要用户确认。<code>default</code> 模式下大部分写操作需弹层确认；<code>auto</code> 模式下 AI 分类器评估风险、低风险自动放行；<code>bypassPermissions</code> 则全量放行。放行后在本地执行工具。</p></li><li><p><strong>结果处理</strong>：工具输出（成功结果或错误）被封装为 <code>tool_result</code> 消息，追加进消息列表。这一步可能触发副作用：写文件后更新 LSP、运行命令后解析输出、调用 MCP 后缓存资源。</p></li><li><p><strong>循环或终止</strong>：<code>QueryEngine</code> 检查本轮 API 返回的 <code>stop_reason</code>——若为 <code>tool_use</code> 且仍有待执行工具，则携带新的 <code>tool_result</code> 发起下一轮流式调用；若为 <code>end_turn</code>（模型认为回答完成），则停止渲染，把控制权交还 REPL 等待下一次输入。整个「继续或终止」的判断完全基于返回值，而非外部循环变量。</p></li></ol><h2 id="七、关键差异-cc-与-opencode-codex-的不同" tabindex="-1">七、关键差异：CC 与 OpenCode / Codex 的不同 <a class="header-anchor" href="#七、关键差异-cc-与-opencode-codex-的不同" aria-label="Permalink to &quot;七、关键差异：CC 与 OpenCode / Codex 的不同&quot;">​</a></h2><p>把 Claude Code 放回横向对比中，有六处差异最能体现其设计取舍：</p><p><strong>1. 源码是否公开</strong>。Claude Code 是三者中唯一未公开源码的实现（本系列基于泄漏源码分析），OpenCode 与 Codex 均在 GitHub 公开。源码未公开带来的直接影响是 <code>feature()</code> 死代码消除被大量使用——Anthropic 可以在同一个代码库里维护尚未公开的特性，打包时按 flag 裁剪出不同产物。这也解释了为何 <code>main.tsx</code> 里能 grep 到近二十个 feature flag：Coordinator、Kairos、SSH_REMOTE、CHICAGO_MCP 等都是仍在灰度、尚未对全部用户开放的能力。源码不公开的代价是社区无法审计实现细节、贡献修复；好处是产品迭代节奏可控、未成熟特性不会过早暴露。</p><p><strong>2. continuation-driven polling 主循环</strong>。这是 Claude Code 最独特的架构选择。OpenCode 走事件驱动 + async 流式，Codex 走 Tokio event loop，二者都有显式的循环结构或事件分发器。Claude Code 也有 <code>while(true)</code>（<code>query.ts:307</code>），但它的特殊之处在于「是否继续下一轮」的决策完全交给 API 返回的 <code>stop_reason</code> 与待执行工具列表——循环体内没有固定步骤序列，而是由 API 响应内容在同一个函数体内分支处理，构造新的 <code>State</code> 后 <code>continue</code> 回顶。这种风格的好处是<strong>没有忙等</strong>——每一轮都 <code>await</code> 流式响应，API 调用间隙进程完全空闲、不消耗 CPU；代价是控制流隐式分散在多个 <code>continue</code> 路径中，调试与可观测性更依赖 <code>profileCheckpoint</code> 这类打点。第 2 篇会详解这套机制及其 7 条 continue 路径，以及对中断、取消、超时的处理。</p><p><strong>3. 无沙箱</strong>。Codex 内置跨平台沙箱（文件系统隔离、命令沙箱化），是三者中安全模型最重的。Claude Code 与 OpenCode 都没有沙箱，转而依赖<strong>权限系统</strong>来约束工具执行。无沙箱意味着工具（如 BashTool）直接以用户权限运行命令，风险完全由权限系统的「确认/放行」决策承担。这是一个重要的取舍：沙箱能兜底防越权，但会牺牲「直接操作用户真实环境」的便利——Claude Code 选择了后者，把安全责任上移到权限决策点。Claude Code 的权限系统是三者中最精细的——下一点详述。</p><p><strong>4. 最精细的权限系统</strong>。Claude Code 有 <strong>7 种权限模式</strong>（5 个外部 <code>default</code> / <code>acceptEdits</code> / <code>plan</code> / <code>bypassPermissions</code> / <code>dontAsk</code>，加 2 个内部 <code>auto</code> / <code>bubble</code>，见 <code>src/types/permissions.ts:16-28</code>），其中用户按 Shift+Tab 循环的实际路径是 <code>default → acceptEdits → plan → bypassPermissions → (auto 若可用 else default)</code>，<code>auto</code> 模式由 AI 转录分类器实时判断每个工具调用是否安全、自动放行低风险操作，被 <code>TRANSCRIPT_CLASSIFIER</code> feature flag 门控（<code>src/utils/permissions/autoModeState.ts</code>）。<code>plan</code> 模式则把 Agent 限制为「只读规划」——可调用查询类工具，但所有写操作需用户在规划完成后批准。<code>dontAsk</code> 与 <code>bubble</code> 是尚未在 UI 暴露的内部模式。相比之下，OpenCode 的权限模型更简单（基于规则的白名单/黑名单），Codex 则把安全重点放在沙箱而非权限分级上。Claude Code 用「多态 + AI 分类器」把权限粒度做到工具调用级别，是其能在无沙箱前提下仍保证一定安全性的关键。</p><p><strong>5. Agent 支持较新</strong>。Codex 原生支持成熟的 Agent Tree（多 agent 编排、父子任务），OpenCode 也有 subagent 机制。Claude Code 的 agent 能力（Coordinator 协调模式、Teammate 进程内协作、Agent Swarms）由 <code>COORDINATOR_MODE</code>、<code>KAIROS</code> 等 feature flag 门控，相对更新、成熟度仍在迭代，本系列第 5 篇专门讨论。这种「Agent 能力仍在灰度」的状态，与其迭代策略一致——大改动先以 feature flag 软发布，验证后再逐步默认启用。</p><p><strong>6. Bun <code>feature()</code> 编译期裁剪</strong>。前文已述，这是 Bun 独有的能力——OpenCode 虽也用 Bun 但未如此重度使用 <code>feature()</code>（其特性切换更多依赖运行期配置而非编译期裁剪），Codex 是 Rust 编译、用条件编译（<code>#[cfg]</code>）实现类似效果但机制完全不同。Claude Code 对 <code>feature()</code> 的重度依赖，使其产物能按发布渠道（稳定版/内测版/企业版）裁剪出不同体积与能力集合，这是成熟产品工程化的重要工具。下表汇总：</p><table tabindex="0"><thead><tr><th>差异点</th><th>Claude Code</th><th>OpenCode</th><th>Codex</th></tr></thead><tbody><tr><td>源码公开</td><td>未公开（泄漏分析）</td><td>开源</td><td>开源</td></tr><tr><td>主循环</td><td>continuation-driven polling</td><td>事件驱动 + async 流式</td><td>Tokio event loop</td></tr><tr><td>沙箱</td><td>无</td><td>无</td><td>跨平台沙箱</td></tr><tr><td>权限模型</td><td>5 外部 + 2 内部模式 + AI 分类器</td><td>简单分级</td><td>沙箱优先</td></tr><tr><td>Agent 成熟度</td><td>较新（feature flag 门控）</td><td>subagent 机制</td><td>原生 Agent Tree</td></tr><tr><td>死代码消除</td><td><code>bun:bundle</code> feature()</td><td>未重度使用</td><td>Rust <code>#[cfg]</code> 条件编译</td></tr></tbody></table><h2 id="八、小结" tabindex="-1">八、小结 <a class="header-anchor" href="#八、小结" aria-label="Permalink to &quot;八、小结&quot;">​</a></h2><p>Claude Code 是一个 51 万行级别的纯客户端终端 Agent：Bun 运行时 + React/Ink 自研渲染层 + continuation-driven 主循环 + 多态权限系统（5 外部 + 2 内部模式）+ 编译期 feature flag 裁剪。它的启动流程把钥匙串与 MDM 读取藏进模块导入时间，初始化严格按配置→认证→特性→工具→MCP 顺序串行推进，最终在 <code>launchRepl</code> 处交棒给查询引擎。</p><p>后续章节将沿这条骨架逐层下钻：第 2 篇剖析 <code>QueryEngine</code> 的 continuation 机制与流式工具调度，第 3 篇展开工具系统，第 4 篇拆解五级压缩，第 5 篇进入 Agent 协作，第 7 篇聚焦权限系统的 AI 分类器。每一篇都回到本文建立的分层与启动框架，从宏观走向微观。</p><p>理解 Claude Code 的最佳路径，是把本文的三个结论作为后续阅读的坐标：其一，它是纯客户端编排器，所有复杂度集中在「如何组织上下文、如何调度工具、如何管控权限」三件事上；其二，它的启动是一个「并行预热 + 串行装配」的过程，<code>profileCheckpoint</code> 打点让每个阶段可观测；其三，它的架构本质是 continuation-driven——没有忙等循环，流程推进完全由 API 返回值驱动。把握住这三点，后续每一篇源码精读都能在正确的抽象层级上展开。</p><h2 id="章节小测" tabindex="-1">章节小测 <a class="header-anchor" href="#章节小测" aria-label="Permalink to &quot;章节小测&quot;">​</a></h2>',29)),s(p,{questions:l})])}}});export{C as __pageData,m as default};
